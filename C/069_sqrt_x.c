/*
Problem: 69. Sqrt(x)

Approach:
Used built-in sqrt() function from math.h to compute square root.
Returned integer part of the result.

Time Complexity: O(1)
Space Complexity: O(1)

Language: C
*/

#include <math.h>

int mySqrt(int x) {
    return sqrt(x);
}
